<!DOCTYPE html>
<html lang="en">
   <% include partials/head %>
   <body>
      <% include partials/popup %>
      <header>
      <% include partials/header %>
      </header>
      <div class="container">
        <div class="row">
          <div class="col-12 text-center mt-4 mb-4">
              <strong style="font-size: 20px;">RETURNS AND EXCHANGES</strong>
          </div>
        </div>
        <div class="row">
            <div class="col-3"></div>
            <% cont.forEach(function(content){ %>
            <div class="col-6">
                
                <%= content.returnexchange %>
            </div>
            <% }) %>
        </div>
    </div>
      <div class="container">
        <div class="row">
          <div class="col-12 text-center mt-4 mb-4">
              <strong style="font-size: 20px;">PAYMENT ACCEPTS</strong>
          </div>
        </div>
        <div class="row">
            <div class="col-3"></div>
            <% cont.forEach(function(content){ %>
            <div class="col-6">
                
                <%= content.payment %>
            </div>
            <% }) %>
        </div>
    </div>
      <footer>
      <% include partials/footer %>
      </footer>
      <a href="#" class="tt-back-to-top">BACK TO TOP</a>
      <% include partials/modal-quickviewmodal %>
      <% include partials/modal-videoproduct %>
      <% include partials/modal-subscribegood %>
      <% include partials/script %>
   </body>
</html>